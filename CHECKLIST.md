# 착수 전 체크리스트

코드를 쓰기 전에 답이 나와 있어야 하는 것들. 위에서부터 순서대로 막힌다.

---

## A. 지금 당장 확인 (30분)

기기 앞에서 직접 해봐야 알 수 있는 것들이다. 여기서 답이 갈리면 설계가 통째로 바뀐다.

### A-1. 아이가 쓸 기기에서 음성이 나오는가

프로토타입을 아이 기기에서 열고 확인한다.

- [ ] 덩어리 읽기 화면에서 소리가 나는가
- [ ] "천천히" 버튼이 실제로 느리게 읽는가
- [ ] 받아쓰기 문제에서 화면 진입 시 자동 재생이 되는가, 아니면 버튼을 눌러야만 나는가
- [ ] 발음이 알아들을 만한가 (기계음이 심하면 아이가 못 받아 적는다)

**나오지 않는다면** — `en-US` 목소리가 없는 경우다. 기기 설정에서 영어 음성을 추가하거나, 받아쓰기 유형을 빼고 두 유형으로 갈지 결정해야 한다.

**iOS라면** — 사용자 제스처 없이는 재생되지 않는다. 자동 재생 구간을 전부 버튼으로 바꿔야 한다.

### A-2. 아이가 영어 키보드를 다룰 수 있는가

- [ ] 한/영 전환을 스스로 하는가
- [ ] 철자를 떠올리는 것과 타이핑하는 것 중 어느 쪽이 더 걸리는가

**타이핑이 병목이면** 화면 안에 알파벳 키패드를 직접 만들어야 한다. 적은 작업이 아니므로 미리 알아야 한다.

### A-3. 30문제를 한 번에 감당하는가

1차 30 + 오답 10 + 최종 30 = 70문제. 시간과 집중력을 재본다.

- [ ] 한 바퀴에 몇 분 걸리는가
- [ ] 중간에 지치는 지점이 어디인가

**지친다면** 15개씩 두 세트로 쪼개는 구조를 처음부터 넣는다. 나중에 붙이면 데이터 모델을 건드려야 한다.

---

## B. 결정 필요

### B-1. DB

| 선택지 | 언제 |
|---|---|
| SQLite (파일) | 로컬에서만 쓸 때. 백업이 파일 복사로 끝난다 |
| Postgres | 아이 기기에서 접속하게 할 때 |
| 인메모리 | 프로토타입 단계. 재시작하면 날아감 |

가족 한 명이 쓰는 앱이므로 SQLite로 시작해도 충분하다. 결정 전까지는 리포지토리 인터페이스로 감싸둘 것.

- [ ] 결정:

### B-2. 호스팅

- [ ] 아빠 노트북에서 실행하고 같은 와이파이로 접속
- [x] 어딘가에 배포해서 언제든 접속 — **Vercel** (`DEPLOY.md`)
- [ ] 기타:

배포를 택했으므로 최소한의 접근 제한이 필요하다 → B-3.

### B-3. 인증

가족 한 명이면 불필요하다. **없이 시작하는 쪽을 권한다.** 배포를 택했다면 URL을 모르게 하는 정도로 충분한지 판단할 것.

현재 구현: 로그인 없음. `APP_PASSCODE` 환경변수를 넣으면 사진 추출(API 키를 쓰는 부분)에만 비밀번호를 요구한다. 기기당 한 번 입력하면 저장된다.

- [ ] 결정: `APP_PASSCODE` 를 넣을 것인가 (권장: 넣는다)

### B-4. 프론트엔드

- [x] 결정: Vite + React. 프로토타입을 `src/App.jsx` 로 옮기고 로직은 `shared/` 에서 import 한다. Next.js 는 서버 상태(DB)가 생기면 다시 검토

### B-5. API 호출 상한

재시도 루프가 돌면 비용이 튄다. 일일 호출 상한을 건다.

현재: 사진 1장당 최대 3회(정상 1회, 잘리면 +2회), JSON 깨짐은 1회만 재시도. 일일 상한은 서버리스가 상태를 공유하지 않아 아직 없다. DB/KV 가 붙을 때 함께 건다.

- [ ] 하루 최대 추출 횟수:
- [ ] 상한 도달 시 동작:

---

## C. 첫 스프린트에 넣을 것

순서가 중요하다. 검수 화면 없이 시험 기능을 먼저 만들면 아이가 틀린 뜻을 외우게 된다.

1. [x] 로직 이식 — `shared/wordlab-logic.mjs`. 테스트는 node 내장 러너 그대로 (Jest 는 NestJS 로 갈 때)
2. [x] `POST /api/extract` — 잘림 감지, JSON 복구, 스키마 검증 4단계 전부
3. [x] **추출 결과 검수 화면** — 인라인 수정, 신뢰도 낮은 항목 표시
4. [ ] WordSet 저장/조회 — 지금은 `localStorage` 한 회차만
5. [x] 문제지 생성 + 채점
6. [x] 1차 → 오답 → 최종 흐름 연결
7. [ ] AnswerLog 기록 (`rawInput` 반드시 포함) — DB 결정 뒤

여기까지가 지금 프로토타입과 동등한 상태다. 그 다음이 누적 오답이다.

---

## D. 검수 화면 요구사항

가장 중요한 화면이므로 따로 적는다.

- [x] 추출 직후, 저장 전에 뜬다
- [x] 단어 / 뜻 / 덩어리가 표로 보이고 각 칸을 눌러 바로 고칠 수 있다
- [x] 다음 항목은 눈에 띄게 표시한다
  - 뜻이 비었거나 한 글자인 항목
  - `chunks`가 fallback으로 대체된 항목 (덩어리가 1개뿐인 긴 단어)
  - 같은 뜻이 두 단어에 중복으로 붙은 항목
- [x] 행 삭제 / 행 추가 가능
- [ ] 부모가 30초 안에 훑고 넘길 수 있다 — 실제 사진으로 재봐야 안다

---

## E. 첫 회차에 확인할 것

실제로 월요일 시험 준비에 써보고 나서 답할 것들.

- [ ] 추출 정확도 — 30개 중 몇 개를 고쳐야 했는가
- [ ] 파닉스 규칙 5개가 쓸 만했는가, 아니면 억지스러웠는가
- [ ] 아이가 도움 없이 6단계를 끝까지 갔는가
- [ ] 어느 화면에서 멈칫했는가
- [ ] 실제 학교 시험 점수가 이전 회차보다 올랐는가

마지막 항목이 이 프로젝트의 유일한 성공 지표다.
