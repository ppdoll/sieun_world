# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 규칙이다.
프로젝트 배경과 기능 명세는 `README.md`를 먼저 읽을 것.

---

## 이 프로젝트가 무엇인가

초등 4학년이 주 3회 치르는 영단어 시험을 준비하는 학습 앱.
**사용자가 아이 한 명이라는 점이 모든 설계 판단의 기준이다.** 확장성, 멀티테넌시, 마이크로서비스 같은 고려는 필요 없다. 대신 아이가 혼자 끝까지 돌 수 있는 UX와, 틀린 답을 외우게 만들지 않는 정확성이 중요하다.

---

## 기술 스택

- **호스팅**: Vercel. 프론트(Vite + React)와 서버리스 함수(`api/`)가 한 프로젝트. 절차는 `DEPLOY.md`
- **서버**: Vercel Node 함수. `api/_lib/handlers.mjs` 가 본체, `api/extract/*.js` 는 얇은 진입점. 회차 누적이 필요해지면 NestJS 로 옮긴다 (README §5)
- **테스트**: node 내장 러너 (`npm test`). NestJS 로 가면 Jest
- **DB**: 미정 (§결정 대기). 지금은 브라우저 `localStorage` 한 회차. 붙일 때는 리포지토리 인터페이스로 감싸 교체 가능하게
- **프론트**: Vite + React (`src/`). 순수 로직은 `shared/` 에서 import

다른 언어를 써야 할 이유가 생기면 먼저 물어볼 것.

---

## 코드를 쓸 때

### 테스트는 선택이 아니다

**코드를 작성하면 반드시 테스트를 함께 작성한다.** 요청에 "테스트도"라고 적혀 있지 않아도 마찬가지다.

- 순수 함수는 예외 없이 단위 테스트
- 컨트롤러/서비스는 최소한 정상 경로 1개 + 실패 경로 1개
- 작성 후 실제로 실행해서 통과를 확인하고, 결과를 보고할 것. "테스트를 작성했습니다"로 끝내지 말 것

### 로직을 프레임워크에서 떼어낸다

채점, 문제지 생성, 힌트 생성, 모델 응답 해석 같은 로직은 프레임워크에 의존하지 않는 순수 함수로 `shared/` 에 둔다.
`shared/wordlab-logic.mjs`, `shared/extract-logic.mjs` 가 그 예다. DOM도 SDK도 모르기 때문에 테스트가 빠르고 프론트와 서버가 함께 쓴다.

모델 호출(`api/_lib/claude.mjs`)과 HTTP 관심사(`api/_lib/handlers.mjs`)는 바깥 껍데기에만 둔다. 흐름 조율(`shared/extract-service.mjs`)은 호출 함수를 주입받아 가짜로 테스트한다.

### 난수는 항상 시드를 받는다

문제지 생성에 `Math.random()`을 직접 쓰지 말 것. `makeRng(seed)`를 통과시킨다.

- 같은 시드로 같은 문제지를 재현할 수 있어야 디버깅과 테스트가 가능하다
- "어제 봤던 그 문제 다시"가 가능해진다

### LLM 응답을 신뢰하지 않는다

`/api/extract` 계열은 외부 모델 응답을 다룬다. `output_config.format` 으로 JSON 스키마를 강제하더라도 다음을 항상 처리한다.

1. `stop_reason === 'max_tokens'` 검사 — 잘렸으면 분할 재시도
2. JSON 파싱 실패 — `extractJson`으로 중괄호 구간 복구 시도
3. 스키마 검증 — 빈 `word`/`meaning` 제거, 중복 제거
4. `chunks` 철자 검증 — 이어붙인 결과가 원본과 다르면 통째로 되돌림

이 네 단계를 건너뛴 추출 코드는 머지하지 않는다. `stop_reason === 'refusal'` 은 재시도하지 않고 사용자에게 다시 찍으라고 안내한다.

### 비밀은 서버 함수에만

`ANTHROPIC_API_KEY` 는 `api/` 에서만 읽는다. `src/` 에서 `import.meta.env` 로 키를 노출하는 코드는 머지하지 않는다. 사진은 함수 안에서 모델에 넘기고 끝낸다. 디스크·로그·외부 저장소에 남기지 않는다.

---

## 도메인 용어

코드와 대화에서 이 단어들을 일관되게 쓴다.

| 용어 | 의미 |
|---|---|
| **WordSet** | 한 회차 분량의 단어장. 사진 한 묶음에서 나온 결과 |
| **chunk** | 단어를 나눈 발음 덩어리. `material` → `["ma","te","ri","al"]` |
| **stage** | 시험 단계. `quiz`(1차) / `review`(오답) / `final`(최종) |
| **type** | 문제 유형. `spell`(뜻→철자) / `listen`(듣고 철자) / `meaning`(단어→뜻 객관식) |
| **hintMask** | 일부 글자만 남긴 힌트. `coral` → `c _ _ a l` |
| **matchedPrefixLen** | 오답에서 앞부터 맞은 글자 수 |
| **WeakWord** | 여러 회차에 걸쳐 반복해서 틀린 단어 |

---

## 사용자 대면 문구를 쓸 때

화면에 나가는 모든 텍스트는 **초등 4학년이 읽는다**는 전제로 쓴다.

- 짧은 문장. 한 문장에 한 가지만
- "제출", "실행", "확인 완료" 같은 시스템 말투 대신 무엇이 일어나는지 그대로: "확인", "다음", "다시 듣기"
- 오답 피드백에 부정적 표현을 쓰지 않는다. "틀렸습니다" 대신 "정답을 볼까요"
- 부분 정답은 반드시 알려준다: "앞의 4글자는 맞았어요"
- 에러 메시지는 무엇을 하면 되는지 알려준다. "오류가 발생했습니다" 금지

---

## 하지 말 것

- 포인트, 뱃지, 랭킹, 연속 출석 같은 게이미피케이션 (README §8 참고)
- 문제 풀이에 제한 시간 걸기
- 이미 맞힌 단어를 반복 출제하기
- 교재 사진 원본을 서버에 저장하기
- 테스트 없이 로직 추가하기

---

## 작업 방식

- 큰 변경 전에 무엇을 바꿀지 먼저 말하고 시작할 것
- 결정이 필요한 지점(DB 선택, 인증 도입 등)은 임의로 정하지 말고 물어볼 것. README §7에 미결 항목이 정리되어 있다
- 커밋은 한 가지 일만 담는다
